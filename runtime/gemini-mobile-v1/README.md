# gemini-mobile-v1

Stable runtime based on the user-approved Gemini travel-page design. `template-parts/` and `js-parts/` are concatenated by `scripts/build.mjs`; do not edit generated `dist/` copies.

The split files are an authoring/storage detail only. Their lexical order is significant, so filenames are zero-padded and must remain sorted.
